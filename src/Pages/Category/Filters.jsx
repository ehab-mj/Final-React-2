import React, { useState } from 'react';

function Filters() {
    const [selectedFilters, setSelectedFilters] = useState([]);
    const [filtersItems, setFiltersItems] = useState(items);
    let filters = ["All", "Action", "Racing", "Sports"]
    return (
        <div>

        </div>
    )
}

export default Filters;
